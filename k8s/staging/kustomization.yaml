apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fitness-api-staging

resources:
  - ../base
  - secret-provider-class.yaml
  - configmap-env.yaml

patches:
  - path: deployment-patch.yaml
